[
  {
    "origin": ["http://localhost:52067", "http://localhost:5000", "https://your-production-web-url.com"],
    "method": ["GET", "POST", "PUT", "DELETE"],
    "maxAgeSeconds": 3600,
    "responseHeader": ["Content-Type", "Authorization"]
  }
]

/**
[
  {
    "origin": [
      "https://arptc-connect.web.app",
      "http://localhost:56455",
      "http://localhost:*"],
    "method": ["GET", "POST", "PATCH", "POST", "OPTIONS", "PUT"],
    "responseHeader": [
      "Content-Type",
      "Authorization",
      "X-Goog-Resumable",
      "X-Upload-Content-Type",
      "Content-Disposition",
      "x-firebase-storage-version",
      "x-goog-upload-protocol"
    ],
    "maxAgeSeconds": 3600
  }
]
**/